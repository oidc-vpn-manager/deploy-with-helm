{{- if and .Values.frontend.enabled .Values.frontend.adminService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "oidc-vpn-manager.frontend.fullname" . }}-admin
  labels:
    {{- include "oidc-vpn-manager.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend-admin
spec:
  {{- if not .Values.frontend.autoscaling.enabled }}
  replicas: {{ .Values.frontend.adminService.replicaCount }}
  {{- end }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      {{- include "oidc-vpn-manager.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: frontend-admin
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        checksum/secret: {{ include (print $.Template.BasePath "/secrets.yaml") . | sha256sum }}
      labels:
        {{- include "oidc-vpn-manager.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: frontend-admin
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "oidc-vpn-manager.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: frontend-admin
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: {{ include "oidc-vpn-manager.image" (dict "registry" .Values.image.registry "repository" .Values.image.repository "component" "frontend" "tag" .Values.frontend.image.tag "Chart" .Chart "Values" .Values) }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.frontend.service.port }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 30
            failureThreshold: 5
          env:
            - name: ENVIRONMENT
              value: {{ .Values.frontend.config.environment | quote }}
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "oidc-vpn-manager.fullname" . }}-db-users
                  key: frontend-uri
            - name: LOG_LEVEL
              value: {{ .Values.frontend.config.logLevel | quote }}
            - name: SIGNING_SERVICE_URL
              value: "http://{{ include "oidc-vpn-manager.signing.fullname" . }}:{{ .Values.signing.service.port }}"
            - name: SIGNING_SERVICE_API_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.signingApiSecret.name | default (printf "%s-signing-api-secret" (include "oidc-vpn-manager.fullname" .)) }}
                  key: {{ .Values.secrets.signingApiSecret.key | default "api-secret" }}
            - name: CERTTRANSPARENCY_SERVICE_URL
              value: "http://{{ include "oidc-vpn-manager.certtransparency.fullname" . }}:{{ .Values.certtransparency.service.port }}/api/v1"
            - name: OIDC_DISCOVERY_URL
              value: {{ .Values.frontend.config.oidc.discoveryUrl | quote }}
            - name: OIDC_CLIENT_ID
              value: {{ .Values.frontend.config.oidc.clientId | quote }}
            - name: OIDC_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.oidcClientSecret.name | default (printf "%s-oidc-client-secret" (include "oidc-vpn-manager.fullname" .)) }}
                  key: {{ .Values.secrets.oidcClientSecret.key | default "client-secret" }}
            - name: OIDC_ADMIN_GROUP
              value: {{ .Values.frontend.config.oidc.adminGroup | quote }}
            - name: OPENVPN_SERVER_HOSTNAME
              value: {{ .Values.frontend.config.openvpnServerHostname | quote }}
            - name: FLASK_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.flaskSecretKey.frontend.name | default (printf "%s-flask-secret-key-frontend" (include "oidc-vpn-manager.fullname" .)) }}
                  key: {{ .Values.secrets.flaskSecretKey.frontend.key | default "flask-secret-key" }}
            - name: FERNET_ENCRYPTION_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.fernet.name | default (printf "%s-fernet" (include "oidc-vpn-manager.fullname" .)) }}
                  key: {{ .Values.secrets.fernet.key | default "fernet-key" }}
            - name: SITE_NAME
              value: {{ .Values.frontend.adminService.siteName | default "VPN Admin Service" | quote }}
            - name: USER_URL_BASE
              value: {{ .Values.frontend.userService.urlBase | quote }}
            - name: OVPN_TEMPLATE_PATH
              value: "/app/openvpn_templates"
            - name: SERVER_TEMPLATES_DIR
              value: "/app/server_templates"
            - name: OVPN_OPTIONS_PATH
              value: "/app/openvpn_options.yaml"
            - name: ROOT_CA_CERTIFICATE_FILE
              value: "/app/pki/root-ca.crt"
            - name: INTERMEDIATE_CA_CERTIFICATE_FILE
              value: "/app/pki/intermediate-ca.crt"
            {{- if or .Values.secrets.tlsCrypt.value .Values.secrets.tlsCrypt.name }}
            - name: OPENVPN_TLS_CRYPT_KEY_FILE
              value: "/app/pki/tls-crypt.key"
            {{- end }}
          resources:
            {{- toYaml .Values.frontend.resources | nindent 12 }}
          volumeMounts:
            - name: pki-certs
              mountPath: /app/pki
              readOnly: true
            {{- if or .Values.secrets.tlsCrypt.value .Values.secrets.tlsCrypt.name }}
            - name: tls-crypt
              mountPath: /app/pki/tls-crypt.key
              subPath: {{ .Values.secrets.tlsCrypt.key | default "tls-crypt.key" }}
              readOnly: true
            {{- end }}
            - name: openvpn-templates
              mountPath: /app/openvpn_templates
              readOnly: true
            - name: server-templates
              mountPath: /app/server_templates
              readOnly: true
            - name: openvpn-options
              mountPath: /app/openvpn_options.yaml
              subPath: openvpn_options.yaml
              readOnly: true
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: pki-certs
          secret:
            secretName: {{ .Values.secrets.pkiCert.name | default (printf "%s-pki-cert" (include "oidc-vpn-manager.fullname" .)) }}
        {{- if or .Values.secrets.tlsCrypt.value .Values.secrets.tlsCrypt.name }}
        - name: tls-crypt
          secret:
            secretName: {{ .Values.secrets.tlsCrypt.name | default (printf "%s-tls-crypt" (include "oidc-vpn-manager.fullname" .)) }}
        {{- end }}
        - name: openvpn-templates
          configMap:
            name: {{ include "oidc-vpn-manager.fullname" . }}-openvpn-templates
        - name: server-templates
          configMap:
            name: {{ include "oidc-vpn-manager.fullname" . }}-server-templates
        - name: openvpn-options
          configMap:
            name: {{ include "oidc-vpn-manager.fullname" . }}-openvpn-options
        - name: tmp
          emptyDir: {}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}